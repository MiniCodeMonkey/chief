{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "cmd/diffs-get.json",
  "title": "Diffs Get Command",
  "description": "Command to retrieve git diffs for a project",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "const": "diffs-get"
    },
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "device_id": {
      "type": "string"
    },
    "timestamp": {
      "type": "string",
      "format": "date-time"
    },
    "payload": {
      "type": "object",
      "properties": {
        "project_id": {
          "type": "string",
          "format": "uuid",
          "description": "ID of the project to get diffs for"
        }
      },
      "required": ["project_id"],
      "additionalProperties": false
    }
  },
  "required": ["type", "id", "device_id", "timestamp", "payload"],
  "additionalProperties": false
}
